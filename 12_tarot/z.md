
------------


このアプリで使われている技術を層ごとに整理して解説します。

## 1. 全体アーキテクチャ

サーバーサイド処理が一切ない、**ブラウザだけで完結するアプリケーション**です。唯一の例外は、初回にモデルファイルをHugging Faceから取得する部分だけで、それ以降の推論（LLMによる文章生成）はすべてローカルのブラウザ内で行われます。

```
ブラウザ
 ├─ HTML/CSS : 見た目とアニメーション
 ├─ JavaScript (ESモジュール) : ロジック全体
 └─ wllama (WebAssembly) : LLM推論エンジン
      └─ GGUFモデルファイル (Hugging Faceからダウンロード)
```

## 2. LLM実行基盤：wllama + WebAssembly

- **WebAssembly (WASM)**：C/C++で書かれたコードをブラウザ上でネイティブに近い速度で実行できる仕組み。今回はllama.cpp（LLM推論エンジン本体、C++製）をWASMにコンパイルしたものを使っています。
- **wllama**：このWASMビルドをJavaScriptから扱いやすくするラッパーライブラリです。モデルのダウンロード、トークン化、推論実行、ストリーミング出力といった一連の流れをAPI化しています。
- **Webワーカー**：実際の推論処理はメインスレッド（画面描画をしているスレッド）とは別の**ワーカースレッド**で動いています。これにより、重い計算をしていてもUIがフリーズしません（コンソールログに出ていた`onmessage`や`postMessage`はこのワーカーとの通信ログです）。
- **マルチスレッド/シングルスレッド**：WASMは本来複数スレッドで並列計算できますが、それには`Cross-Origin-Opener-Policy`等の特殊なHTTPヘッダーが必要です。今回の簡易サーバーではそれが無かったため、シングルスレッドにフォールバックしていました。

## 3. モデルの実体：GGUFフォーマットと量子化

- **GGUF**：llama.cpp系エコシステムで使われるモデルファイル形式。重み・トークナイザ・メタデータを1ファイルにまとめたバイナリです。
- **量子化 (Quantization)**：元々32bit/16bitの浮動小数点で持っている数十億個のパラメータを4bit程度に圧縮する技術。`Q4_K_M`は「4bit・K-quant方式・Mバリアント」を意味し、精度をなるべく保ちながらファイルサイズを1/4程度に抑えています。これのおかげで1.5Bパラメータのモデルが約1GBに収まっています。

## 4. 推論の流れ：プロンプト整形とストリーミング

- **ChatML形式**：Qwen系モデルが学習時に使っている対話フォーマットです。`<|im_start|>system ... <|im_end|>`のように役割ごとにタグで囲むことで、モデルに「これはシステム指示」「これはユーザー発言」と認識させます。今回はこれを手動で組み立ててから`createCompletion()`に渡しています。
- **トークンストリーミング**：生成は一気に返ってくるのではなく、1トークンずつ生成されるたびに`onNewToken`コールバックが呼ばれます。これを使って、生成中の文章をリアルタイムに画面へ反映しています（ChatGPT等でおなじみの「文字が流れるように表示される」演出の仕組みです）。
- **サンプリングパラメータ**：`temp`（温度、値が高いほど出力がランダムに）、`top_k`/`top_p`（候補となる単語の絞り込み方）で、生成文の多様性を制御しています。

## 5. フロントエンド側の技術

- **CSS変数（カスタムプロパティ）**：`--gold`や`--ink`のような色をまとめて定義し、テーマ変更をしやすくしています。
- **3D CSSアニメーション（カードめくり）**：`transform-style: preserve-3d`と`rotateY()`を使い、カードの裏表を実際に回転させているように見せています。`backface-visibility: hidden`で、裏返っている面が透けて見えないようにしています。
- **Google Fonts**：見出しに明朝体（Shippori Mincho）、解釈文にセリフ体（Cormorant Garamond）を使い分け、占いらしい静かな雰囲気を演出しています。
- **ESモジュール (`<script type="module">`)**：`import`文でCDN上のライブラリを直接読み込めるようにする、ビルド不要でモダンなJSを書くための仕組みです。

## 6. データ

タロットカード自体のデータ（22枚の大アルカナ、名前・キーワード）はLLMを使わず、ただのJavaScript配列として埋め込んでいます。カードの抽選も`Math.random()`によるシンプルな乱数選択で、LLMが担当しているのは「引いたカードの意味を悩みに寄せて解釈する文章生成」の部分だけです。

---

要するに、**「llama.cppをブラウザで動かす（wllama/WASM）」「軽量化されたモデルファイル（GGUF量子化）」「サーバー不要のクライアントサイドJS/CSS」**という3つの技術の組み合わせで、外部にデータを送らずに完結する占いアプリを実現しています。


--------------

python3 -m http.server 8000

http://localhost:8000/tarot-oracle.html

