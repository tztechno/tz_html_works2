# Neon Mirage — 使用マニュアル

Neon Mirageは、ブラウザだけで遊べるVelvet Strip Casino発のネオン輝く5リール・ビデオスロットです。インストールは不要で、クレジットに実際の金銭価値はありません。9本のラインを同時に狙い、ワイルドを追いかける楽しさそのものが目的のゲームです。

## 起動方法

HTMLファイル(または公開ページ)をパソコン・スマートフォンどちらのブラウザでも開けます。マーキーが点灯するオープニング演出のついたキャビネット、ガラス越しの5×3リール、その下のコントロールデッキが表示されます。スマートフォンでは、キャビネットの上に「本体 / 配当表」の切り替えタブが表示されます(詳しくは後述)。

## スピンのやり方

コントロールデッキ上の**SPIN**ボタンをクリックするか、キーボードの**スペースキー**または**エンターキー**を押します。スピンごとに合計ベット額がクレジットから差し引かれ、リールが回り始めた瞬間に消費されます。5本のリールは同時に回転を始め、左から右へ順番に止まっていきます。

## 判定結果の見方

Neon Mirageは中央列だけでなく、**9本の固定ペイライン**を同時に判定します。上段・中段・下段を貫く直線に加え、行を行き来するジグザグ型やV字型のパターンが6種類あります。どのラインがどのマスを判定するかは、**配当表**タブの図で確認できます。

各ラインは、**一番左のリール**から連続して同じシンボルが3つ・4つ・5つ揃うと配当になります。途中(たとえば3リール目)で途切れると、その先は数えられません。**WILD**シンボル(ピンクからシアンのグラデーションの星形)は、連続の中でどのシンボルの代わりにもなります。

当たると、該当するマスがゴールドのリングで光り、結果表示帯がゴールドに変わって獲得額と的中したライン数を表示します。ハズレの場合は結果表示帯が淡いピンクになり、「ハズレ — もう一度」と表示されます。

## ベットの設定

ベットは合計額ではなく**ライン単位**で設定します。−/+ボタンで**ラインベット**を1〜20クレジットの範囲で調整できます。9本のラインは常にすべて有効なため、もう一方の表示窓に出る**合計ベット額**は常にラインベットの9倍になります。**MAX**ボタンを押すと、その時点で9ライン分を賭けられる最大のラインベットに一気に設定されます。

## 配当表

**配当表**タブを開く(または画面が広い場合はキャビネットの右側を見る)と、各シンボルの3つ揃い・4つ揃い・5つ揃いの配当倍率、9本のペイラインの図、WILDの説明が確認できます。出現しにくいシンボルほど高配当です。

| シンボル | ×3 | ×4 | ×5 |
|---|---|---|---|
| チェリー | 2 | 5 | 15 |
| レモン | 3 | 8 | 20 |
| ベル | 5 | 12 | 30 |
| ホースシュー | 8 | 20 | 50 |
| ダイヤ | 12 | 30 | 80 |
| バー | 20 | 50 | 150 |
| セブン | 40 | 100 | 400 |

上表の倍率はすべて**合計ベット額ではなくラインベット**に掛けられます。たとえばラインベット2でチェリー3つ揃いのラインは4クレジットの配当となり、これが的中したライン1本ごとに加算されます。1回のスピンで複数のラインが同時に的中することもあり、その場合はすべての配当が合算されます。

## クレジットが足りなくなったら

9ラインを最低ベットで賭けるのに必要なクレジットを下回ると、コントロールデッキに**REFILL**ボタンが表示されます。タップするとクレジットが500に補充されます。実際の金銭のやり取りは一切発生しません。

## サウンド

コントロールデッキ下部に「sound: on/off」の切り替えがあります。リール停止音や当たり時のチャイム音をミュートでき、最後に選んだ状態が記憶されます。

## スマートフォンでの表示

画面幅が780px未満になると、配当表は独立したページになります。キャビネットの上に**本体**と**配当表**の2つのタブが表示され、それぞれが画面いっぱいに表示されます(横並びで窮屈になるのを防ぐためです)。タブレットやパソコンなど画面が広い場合は、タブは消えて両方が並んで表示されます。

## 進行状況の保存について

所持クレジット・ラインベット・サウンド設定は、そのブラウザ・その端末にのみ保存されます(ブラウザのローカルストレージを使用)。同じページを同じブラウザで開けば続きから遊べますが、別のブラウザや別の端末には引き継がれません。また、そのブラウザのサイトデータを消去すると、初期値のクレジット500にリセットされます。

## 最後に

Neon Mirageに実際の金銭価値はなく、ギャンブルではありません。あくまでテーマ性を楽しむための玩具です。気軽に楽しんでください。クレジットが尽きても、REFILLボタンがいつでもそこにあります。
